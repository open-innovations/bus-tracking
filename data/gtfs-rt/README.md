# About

Live location data every minute grouped by date.

Each folder contains binary (`.bin`) files which can be parsed by `gtfs_realtime_pb2` in python.

NOTE: These are gitignored for now due to extremely large file sizes. 1 day's data is ~1GB. Please contact us directly if you're interested in the data we collected.
